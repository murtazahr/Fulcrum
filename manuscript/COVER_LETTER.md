# Cover Letter — ACM Transactions on Privacy and Security

Professor Michael Waidner
Editor-in-Chief, ACM Transactions on Privacy and Security
ATHENE / TU Darmstadt / Fraunhofer SIT

Dear Professor Waidner,

We submit **"Topology-Aware Differential Privacy in Hierarchical Federated Learning"** for
consideration as a regular paper in ACM Transactions on Privacy and Security.

## The problem

Hierarchical federated learning places regional aggregators between clients and the cloud, so
a participant's update reaches any observer above that tier only after it has been combined
with its neighbours'. The concealment this provides is not uniform: it depends on the
effective size of the aggregation region, and regions in real deployments differ by an order
of magnitude. Prevailing practice nevertheless applies one noise multiplier to every
participant, calibrated for the most exposed region. Every other participant then carries more
noise than its own exposure requires, and the whole federation pays for that surplus in
accuracy.

## What we contribute

We give a silo-level differential privacy guarantee for the mechanism, and separately bound
the mutual information between a participant's local class distribution and any estimate an
observer above the regional tier can form. The two results are kept apart deliberately: they
rest on different assumptions and answer different questions, and neither is derived from the
other. Throughout, the adjacency used by the mechanism is matched to the granularity of the
property being protected, which is a dataset-level rather than a record-level quantity.

Minimising the worst-case value of that bound under a fixed utility budget yields an explicit,
min-max optimal allocation. The budget it recovers relative to uniform noise has a closed
form, δ = 1 − ⟨ρ⟩_V / ρ_max, which depends only on the region structure and the aggregation
weights. A practitioner can therefore evaluate before training whether the method will help,
and δ is exactly zero when all regions are equally exposed, which supplies an explicit
applicability test rather than a claim of universal benefit. Experiments on image and text
classification confirm the prediction at ε = 0.99, including a balanced control deployment for
which the theory predicts parity with the baseline and which delivers it.

## Why this belongs in TOPS

Two lines of work in the journal bear directly on this paper, and to our reading they have so
far run in parallel.

The first is differentially private mechanism design with formal guarantees, represented by
Zhang, Khalili and Liu on real-time release of sequential data (26(1), 2023,
doi:10.1145/3544837) and by Clifton et al. on private k-nearest-neighbour imputation (25(3),
2022, doi:10.1145/3507952). This work treats calibration rigorously, but outside federated
settings.

The second is privacy and security in federated learning, which has grown considerably in the
journal: Zheng et al. on poisoning conducted under the cover of differential privacy (28(1),
2025, doi:10.1145/3702325), Syros et al. on backdoor attacks in peer-to-peer federated
learning (28(1), 2025), Zheng et al. on defending federated learning against poisoning (28(4),
2025, doi:10.1145/3765743), and Xun et al. on vertical federated learning for private data
publication (29(2), 2026, doi:10.1145/3776587). This work is predominantly adversarial, and
takes the calibration of the noise as given.

Our paper joins the two. It asks how differentially private noise should be calibrated to the
structure of a federated deployment, and answers in closed form. The relationship to Zheng et
al. is the most direct: DP-Poison shows that an adversary can conceal itself within the noise
that differential privacy introduces, treating the magnitude of that noise as fixed. We ask
the prior question of how much noise each participant should carry, and show that the
prevailing answer systematically over-protects most of the federation.

## Originality, prior dissemination, and artefacts

The manuscript is original, is not under review elsewhere, and has not been published in whole
or in part. [ACTION: if an earlier version of this work is publicly posted as a preprint,
state that here with its identifier, per ACM's prior-publication policy.]

All experimental artefacts are archived, and every table and figure in the paper is
regenerated by a script in the accompanying repository. A verification script re-derives each
reported privacy parameter from the mechanism rather than reading a stored value, so the
reported guarantees can be checked independently of our own tooling.

We look forward to the reviewers' assessment.

Sincerely,

Murtaza Rangwala, Richard O. Sinnott, and Rajkumar Buyya
School of Computing and Information Systems
The University of Melbourne, Australia
